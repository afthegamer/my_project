<!--parameter to display the error message-->
<?php if (isset($errors) && count($errors) > 0) : ?>
    <div class="alert alert-danger" role="alert">
      <ul>
        <?php foreach ($errors as $errors) : ?>
          <li class="erreur"><?= $errors; ?></li>
        <?php endforeach; ?>
    </div>
<?php endif; ?>